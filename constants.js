const errorMessages = {
    BAD_REQUEST: 'The body of the request does not contain the string attribute "text"',
    NOT_FOUND: 'The message with the requested ID was not found.'
}

module.exports = { errorMessages }
